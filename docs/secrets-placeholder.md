# Secrets Placeholder

Store the following repository secrets via the GitHub UI or API before deploying workflows that depend on them:

- `DATABASE_URL`
- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`
- `NEXTAUTH_SECRET`
- `OCR_SERVICE_URL`

Values should be provided securely in the repository secrets settings.
